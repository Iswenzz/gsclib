The package cgsc is header only and can be used from CMake via:

	find_package(libuv CONFIG REQUIRED)
	target_link_libraries(main PRIVATE libuv::uv_a)

	find_path(CGSC_INCLUDE_DIRS "CGSC/cgsc.h")
	target_include_directories(main PRIVATE ${CGSC_INCLUDE_DIRS})
